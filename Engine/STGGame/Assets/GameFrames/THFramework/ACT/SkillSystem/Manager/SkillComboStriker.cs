using UnityEngine;

namespace THGame
{
    //可能是技能触发,也可能是受击,冲刺等
    //触发技能,如果X秒没有触发下一个,则可能直接进入冷却
    public class SkillComboStriker : MonoBehaviour
    {
        //触发方式:点按,长按蓄力(可能根据蓄力程度不同触发的不一样),状态进入(有超时限制
    }
}